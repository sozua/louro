# =============================================================================
# Louro — AI Code Reviewer for GitHub PRs
# Copy this file to .env and fill in the values.
# =============================================================================

# -----------------------------------------------------------------------------
# Required — GitHub App credentials
# -----------------------------------------------------------------------------
GITHUB_APP_ID=
GITHUB_WEBHOOK_SECRET=
# Provide either the PEM contents inline or a path to the file (not both):
GITHUB_PRIVATE_KEY=
# GITHUB_PRIVATE_KEY_PATH=./your-app.private-key.pem

# -----------------------------------------------------------------------------
# Conditionally required — at least one AI provider
# Set MODEL_PROVIDER below, then fill in the matching key.
# -----------------------------------------------------------------------------
# Anthropic (default provider)
ANTHROPIC_API_KEY=

# Google Gemini
# GOOGLE_API_KEY=

# AWS Bedrock — uses the default credential chain if not set
# AWS_REGION=us-east-1

# -----------------------------------------------------------------------------
# Optional — model configuration (sensible defaults shown)
# -----------------------------------------------------------------------------
MODEL_PROVIDER=anthropic          # anthropic | bedrock | gemini
PRIMARY_MODEL_ID=claude-sonnet-4-5-20250929
STANDARD_MODEL_ID=claude-sonnet-4-5-20250929
CLASSIFIER_MODEL_ID=claude-haiku-4-5-20251001

# -----------------------------------------------------------------------------
# Optional — database (run `make db` to start postgres in docker)
# Use localhost:5433 when running the app locally, db:5432 inside docker compose
# -----------------------------------------------------------------------------
DATABASE_URL=postgresql+asyncpg://reviewer:reviewer@localhost:5433/louro

# -----------------------------------------------------------------------------
# Optional — logging
# LOG_FORMAT: "text" (default, human-readable) or "json" (structured, for production)
# LOG_LEVEL: standard Python log level (DEBUG, INFO, WARNING, ERROR)
# -----------------------------------------------------------------------------
LOG_FORMAT=text
LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Optional — API key for management endpoints (repos, billing, orgs)
# When set, all management endpoints require X-API-Key header. Leave empty to
# disable authentication (not recommended for production).
# -----------------------------------------------------------------------------
# API_KEY=your-secret-api-key
